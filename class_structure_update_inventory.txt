# Cấu trúc các lớp liên quan đến Use case "Yêu cầu cập nhật số lượng hàng trong kho"

Tài liệu này trích xuất chi tiết cấu trúc các lớp (Class) tham gia vào luồng nghiệp vụ cập nhật tồn kho từ phía tác nhân Site, phục vụ việc vẽ Class Diagram và Sequence Diagram.

---

### 1. Tầng View (Boundary)

#### **Class: SiteDashboard**
- **Package:** `com.importorder.boundary`
- **Kế thừa:** `DashboardBase`
- **Thuộc tính:**
    - `private final OverseasOrderController overseasOrderController`
    - `private final SiteRepository siteRepository`
    - `private final SiteMerchandiseRepository siteMerchandiseRepository`
    - `private final SiteUserRepository siteUserRepository`
    - `private final MerchandiseRepository merchandiseRepository`
- **Phương thức:**
    - `public SiteDashboard(Stage stage, AppContext context)`
    - `protected VBox buildSidebar()` : `VBox`
    - `protected void setInitialPage()` : `void`
    - `private String getMySiteCode() throws SQLException` : `String`
    - `private Node buildOrdersPage()` : `Node`
    - `private void renderOrderDetail(VBox detailBox, SiteOrderDTO order, boolean allowUpdateStatus)` : `void`
    - `private SiteOrderDTO reloadMyOrder(int orderId) throws SQLException` : `SiteOrderDTO`
    - `private Node buildMySitePage()` : `Node`
    - `private Node buildInventoryUpdateRequestsPage()` : `Node`
    - `private Node buildMerchandisePage()` : `Node`
    - `private VBox field(String label, Node node)` : `VBox`
    - `private void reloadSiteMerchandiseTable(TableView<SiteMerchandise> table, String siteCode) throws SQLException` : `void`
    - `private Label chip(String text, String background, String color)` : `Label`
    - `private Label buildStatusChip(InventoryUpdateRequestDTO request)` : `Label`
- **Dependencies:** `OverseasOrderController`, `SiteRepository`, `SiteMerchandiseRepository`, `SiteUserRepository`, `MerchandiseRepository`, `InventoryUpdateFormDialog`, `InventoryUpdateRequestDTO`, `SiteMerchandise`.

#### **Class: InventoryUpdateFormDialog**
- **Package:** `com.importorder.boundary`
- **Thuộc tính:**
    - `private final Stage owner`
    - `private final InventoryUpdateRequestDTO request`
    - `private final Map<String, SiteMerchandise> currentInventoryByCode`
- **Phương thức:**
    - `InventoryUpdateFormDialog(Stage owner, InventoryUpdateRequestDTO request, Map<String, SiteMerchandise> currentInventoryByCode)`
    - `Optional<SiteMerchandise> showDialog()` : `Optional<SiteMerchandise>`
    - `private VBox field(String label, Node node)` : `VBox`
    - `private String safe(String value)` : `String`
- **Dependencies:** `InventoryUpdateRequestDTO`, `SiteMerchandise`.

---

### 2. Tầng Controller

#### **Class: OverseasOrderController**
- **Package:** `com.importorder.controller`
- **Thuộc tính:**
    - `private final OverseasOrderService overseasOrderService`
    - `private final SaleRequestService saleRequestService`
    - `private User currentUser`
    - `private String currentSiteCode`
- **Phương thức:**
    - `public void setCurrentUser(User currentUser)` : `void`
    - `public void setCurrentSiteCode(String siteCode)` : `void`
    - `public ProcessResult processSaleRequest(int saleRequestId) throws IllegalStateException, SQLException` : `ProcessResult`
    - `public List<SiteOrderDTO> getSiteOrdersBySaleRequest(int saleRequestId) throws SQLException` : `List<SiteOrderDTO>`
    - `public List<SiteOrderDTO> getAllSiteOrders() throws SQLException` : `List<SiteOrderDTO>`
    - `public SiteOrderDTO createManualSiteOrder(...) throws SQLException` : `SiteOrderDTO`
    - `public List<SiteOrderDTO> getMySiteOrders() throws SQLException` : `List<SiteOrderDTO>`
    - `public void updateDeliveryStatus(int siteOrderId, SiteOrder.Status newStatus) throws IllegalStateException, SQLException` : `void`
    - `public List<InventoryUpdateRequestDTO> getMyInventoryUpdateRequests() throws SQLException` : `List<InventoryUpdateRequestDTO>`
    - `public void completeInventoryUpdateRequest(int inventoryUpdateRequestId, SiteMerchandise updatedInventory) throws SQLException` : `void`
    - `private void requireRole(User.Role required)` : `void`
    - `private void requireAnyRole(User.Role... roles)` : `void`
- **Dependencies:** `OverseasOrderService`, `SaleRequestService`, `User`, `InventoryUpdateRequestDTO`, `SiteMerchandise`.

---

### 3. Tầng Service

#### **Class: SaleRequestService**
- **Package:** `com.importorder.service`
- **Thuộc tính:**
    - `private final SaleRequestRepository saleRequestRepo`
    - `private final SaleRequestItemRepository saleRequestItemRepo`
    - `private final MerchandiseRepository merchandiseRepo`
    - `private final UserRepository userRepo`
    - `private final SiteRepository siteRepo`
    - `private final SiteMerchandiseRepository siteMerchandiseRepo`
    - `private final InventoryUpdateRequestRepository inventoryUpdateRequestRepo`
- **Phương thức:**
    - `public SaleRequestDTO createRequest(int createdByUserId, List<SaleRequestItemDTO> items) throws SQLException` : `SaleRequestDTO`
    - `public List<SaleRequestDTO> getRequestsByUser(int userId) throws SQLException` : `List<SaleRequestDTO>`
    - `public List<SaleRequestDTO> getAllRequests() throws SQLException` : `List<SaleRequestDTO>`
    - `public void requestCancellation(int requestId, int requestedByUserId, String requestReason) throws SQLException` : `void`
    - `public void requestSitesToUpdateInventoryForMerchandise(String merchandiseCode) throws SQLException` : `void`
    - `public List<InventoryUpdateRequestDTO> getInventoryUpdateRequestsBySite(String siteCode) throws SQLException` : `List<InventoryUpdateRequestDTO>`
    - `public void completeInventoryUpdateRequest(int inventoryUpdateRequestId, String siteCode, SiteMerchandise updatedInventory) throws SQLException` : `void`
    - `private SaleRequestDTO buildDTO(SaleRequest request, List<SaleRequestItem> items) throws SQLException` : `SaleRequestDTO`
    - `private InventoryUpdateRequestDTO buildInventoryUpdateRequestDTO(InventoryUpdateRequest request) throws SQLException` : `InventoryUpdateRequestDTO`
- **Dependencies:** `SaleRequestRepository`, `SaleRequestItemRepository`, `MerchandiseRepository`, `UserRepository`, `SiteRepository`, `SiteMerchandiseRepository`, `InventoryUpdateRequestRepository`, `InventoryUpdateRequestDTO`, `SiteMerchandise`.

---

### 4. Tầng Repository

#### **Class: InventoryUpdateRequestRepository**
- **Package:** `com.importorder.repository`
- **Phương thức:**
    - `private InventoryUpdateRequest mapRow(ResultSet rs) throws SQLException` : `InventoryUpdateRequest`
    - `public List<InventoryUpdateRequest> findByMerchandiseCode(String merchandiseCode) throws SQLException` : `List<InventoryUpdateRequest>`
    - `public List<InventoryUpdateRequest> findBySiteCode(String siteCode) throws SQLException` : `List<InventoryUpdateRequest>`
    - `public Optional<InventoryUpdateRequest> findById(int id) throws SQLException` : `Optional<InventoryUpdateRequest>`
    - `public boolean existsByMerchandiseCode(String merchandiseCode) throws SQLException` : `boolean`
    - `public int insert(Connection conn, InventoryUpdateRequest request) throws SQLException` : `int`
    - `public void markCompleted(Connection conn, int id) throws SQLException` : `void`
- **Dependencies:** `InventoryUpdateRequest`.

#### **Class: SiteMerchandiseRepository**
- **Package:** `com.importorder.repository`
- **Phương thức:**
    - `private SiteMerchandise mapRow(ResultSet rs) throws SQLException` : `SiteMerchandise`
    - `public List<SiteMerchandise> findBySiteCode(String siteCode) throws SQLException` : `List<SiteMerchandise>`
    - `public Optional<SiteMerchandise> findBySiteAndMerchandise(String siteCode, String merchandiseCode) throws SQLException` : `Optional<SiteMerchandise>`
    - `public List<SiteMerchandise> findAvailableSitesForMerchandise(String merchandiseCode) throws SQLException` : `List<SiteMerchandise>`
    - `public void insert(SiteMerchandise sm) throws SQLException` : `void`
    - `public void insert(Connection conn, SiteMerchandise sm) throws SQLException` : `void`
    - `public void updateQuantity(String siteCode, String merchandiseCode, int newQuantity) throws SQLException` : `void`
    - `public void update(SiteMerchandise siteMerchandise) throws SQLException` : `void`
    - `public void update(Connection conn, SiteMerchandise siteMerchandise) throws SQLException` : `void`
    - `public void decreaseQuantity(Connection conn, String siteCode, String merchandiseCode, int amount) throws SQLException` : `void`
    - `public void delete(String siteCode, String merchandiseCode) throws SQLException` : `void`
- **Dependencies:** `SiteMerchandise`.

#### **Class: MerchandiseRepository**
- **Package:** `com.importorder.repository`
- **Phương thức:**
    - `private Merchandise mapRow(ResultSet rs) throws SQLException` : `Merchandise`
    - `public Optional<Merchandise> findByCode(String code) throws SQLException` : `Optional<Merchandise>`
    - `public Optional<Merchandise> findById(int id) throws SQLException` : `Optional<Merchandise>`
    - `public List<Merchandise> findAll() throws SQLException` : `List<Merchandise>`
    - `public List<Merchandise> findByNameContaining(String keyword) throws SQLException` : `List<Merchandise>`
    - `public List<Merchandise> searchByKeyword(String keyword) throws SQLException` : `List<Merchandise>`
    - `public int insert(Merchandise merchandise) throws SQLException` : `int`
    - `public void update(Merchandise merchandise) throws SQLException` : `void`
    - `public void delete(String code) throws SQLException` : `void`
- **Dependencies:** `Merchandise`.

---

### 5. Tầng DTO

#### **Class: InventoryUpdateRequestDTO**
- **Package:** `com.importorder.dto`
- **Thuộc tính:**
    - `private int id`
    - `private String merchandiseCode`
    - `private String merchandiseName`
    - `private String merchandiseUnit`
    - `private String siteCode`
    - `private String siteName`
    - `private InventoryUpdateRequest.Status status`
    - `private LocalDateTime requestedAt`
    - `private LocalDateTime respondedAt`
- **Phương thức:** (Getter/Setter cho tất cả các thuộc tính)

---

### 6. Tầng Entity

#### **Class: InventoryUpdateRequest**
- **Package:** `com.importorder.entity`
- **Cấu trúc nội bộ:**
    - `public enum Status { PENDING, COMPLETED }`
- **Thuộc tính:**
    - `private int id`
    - `private String merchandiseCode`
    - `private String siteCode`
    - `private Status status`
    - `private LocalDateTime requestedAt`
    - `private LocalDateTime respondedAt`
- **Phương thức:** (Getter/Setter cho tất cả các thuộc tính)

#### **Class: SiteMerchandise**
- **Package:** `com.importorder.entity`
- **Thuộc tính:**
    - `private String siteCode`
    - `private String merchandiseCode`
    - `private int quantity`
    - `private String unit`
- **Phương thức:** (Getter/Setter cho tất cả các thuộc tính, `toString()`)

#### **Class: Merchandise**
- **Package:** `com.importorder.entity`
- **Thuộc tính:**
    - `private int id`
    - `private String code`
    - `private String name`
    - `private String unit`
- **Phương thức:** (Getter/Setter cho tất cả các thuộc tính, `toString()`)
